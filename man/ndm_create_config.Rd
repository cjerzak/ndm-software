% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{ndm_create_config}
\alias{ndm_create_config}
\alias{print.ndm_config}
\title{Create runtime configuration objects}
\usage{
ndm_create_config(
  model_type = c("DecoderOnly", "NeuralODE"),
  backbone = "transformer",
  float_type = c("32", "64"),
  force_to_gpu = NULL,
  resave_tfrecords = FALSE,
  gpu_mem_frac = NULL,
  neuralode_local_latent_dim = NULL,
  neuralode_global_latent_dim = NULL,
  neuralode_init_state_logit_offset = NULL,
  neuralode_init_state_logit_scale_max = Inf,
  neuralode_optim_solver = c("tsit5", "dopri8"),
  neuralode_optim_dt0 = 0.001,
  neuralode_optim_controller = c("pid", "constant"),
  neuralode_optim_rtol = 1e-05,
  neuralode_optim_atol = 1e-07,
  enable_kv_cache = TRUE,
  enable_kv_cache_training = TRUE,
  inference_mc_draws = 5L,
  observation_scale_floor = 1e-05,
  initial_observation_scale = 1,
  neuralode_variational = TRUE,
  neuralode_kl_weight = 1,
  neuralode_mean_loss_weight = 0,
  training_objective = c("student_t_nll", "scaled_mse"),
  outcome_loss_scale = NULL,
  compute_backend = c("auto", "cpu", "gpu"),
  ...
)

\method{print}{ndm_config}(x, ...)
}
\arguments{
\item{model_type}{Model family metadata. Use `"DecoderOnly"` or
`"NeuralODE"`.}

\item{backbone}{Backbone family. Phase 1 supports `"transformer"` only.}

\item{float_type}{Floating point precision used when initializing the backend.
Use `"32"` or `"64"`.}

\item{force_to_gpu}{Deprecated logical alias for `compute_backend`. `TRUE`
maps to `"gpu"` and `FALSE` maps to `"cpu"`.}

\item{resave_tfrecords}{Logical scalar preserved for compatibility. Public
training APIs require `FALSE`; prepare canonical inputs before training.}

\item{gpu_mem_frac}{Optional finite GPU memory fraction in `(0, 1]`. It is
invalid when `compute_backend = "cpu"` and rejected at initialization if
`"auto"` resolves to CPU.}

\item{neuralode_local_latent_dim}{Optional local NeuralODE latent width. When
`NULL`, runtime code resolves this to `ModelDims`.}

\item{neuralode_global_latent_dim}{Optional global NeuralODE latent width.
When `NULL`, runtime code resolves this to `ModelDims`.}

\item{neuralode_init_state_logit_offset}{Optional init-state logit offset
vector for the NeuralODE state simplex initializer.}

\item{neuralode_init_state_logit_scale_max}{Scalar cap applied to the
NeuralODE init-state logit scale.}

\item{neuralode_optim_solver}{NeuralODE optimization solver name. Use
`"tsit5"` or `"dopri8"`.}

\item{neuralode_optim_dt0}{Initial step size used by the NeuralODE
optimization solve.}

\item{neuralode_optim_controller}{NeuralODE optimization stepsize controller.
Use `"pid"` or `"constant"`.}

\item{neuralode_optim_rtol}{Relative tolerance used when
`neuralode_optim_controller = "pid"`.}

\item{neuralode_optim_atol}{Absolute tolerance used when
`neuralode_optim_controller = "pid"`.}

\item{enable_kv_cache}{Logical scalar controlling DecoderOnly KV caching.
The default enables the faster cached inference path.}

\item{enable_kv_cache_training}{Logical scalar controlling KV caching during
DecoderOnly gradient training. The default is `TRUE`: the cached training
rollout is gradient-equivalent to the un-cached reference (verified to
float32 tolerance across production topologies, left padding, and mask
holes) and several times faster per training step at production batch
sizes. Set `FALSE` to reproduce historical un-cached training exactly.}

\item{inference_mc_draws}{Positive integer number of posterior draws
requested for inference. Deterministic NeuralODE and DecoderOnly inference
reduce this to one effective draw downstream.}

\item{observation_scale_floor}{Finite positive lower bound added to learned
observation scales.}

\item{initial_observation_scale}{Finite positive initial observation-scale
value on the outcome scale. The default is `1`, matching the unit-scale
initialization used before this control became configurable. It must be
greater than `observation_scale_floor`.}

\item{neuralode_variational}{Logical scalar. When `FALSE`, NeuralODE
training uses deterministic posterior means instead of stochastic
variational samples.}

\item{neuralode_kl_weight}{Non-negative scalar KL multiplier used only when
`neuralode_variational = TRUE`.}

\item{neuralode_mean_loss_weight}{Non-negative multiplier for an auxiliary
masked mean-squared-error term applied to NeuralODE forecasts alongside
the Student-t likelihood.}

\item{training_objective}{Training loss for observed outcomes. The default,
`"student_t_nll"`, preserves the probabilistic likelihood. Use
`"scaled_mse"` for deterministic scale-normalized squared error.}

\item{outcome_loss_scale}{Optional finite positive scalar or outcome-length
vector used to normalize residuals for `training_objective = "scaled_mse"`.
It is required for scaled MSE and otherwise must be `NULL`.}

\item{compute_backend}{Compute device policy: `"auto"` selects a supported
JAX GPU when available and otherwise CPU, while `"cpu"` and `"gpu"`
require the named backend.}

\item{...}{Unused; included for S3 method compatibility.}

\item{x}{An `ndm_config` object.}
}
\value{
`ndm_create_config()` returns an object of class `ndm_config`.

`print.ndm_config()` prints a brief summary of an `ndm_config`
  object and invisibly returns `x`.
}
\description{
Configuration objects collect the runtime options that are threaded through
the higher-level orchestration helpers such as `ndm_prepare_runtime()` and
`ndm_fit()`.
}
\examples{
cfg <- ndm_create_config()
print(cfg)

}
