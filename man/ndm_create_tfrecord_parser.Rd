% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tfrecord.R
\name{ndm_create_tfrecord_parser}
\alias{ndm_create_tfrecord_parser}
\alias{ndm_read_tfrecord_dataset}
\alias{ndm_collect_tfrecord_batches}
\title{Create and read TensorFlow TFRecord datasets}
\usage{
ndm_create_tfrecord_parser(field_names, dtype_map = NULL, tensorflow = NULL)

ndm_read_tfrecord_dataset(
  file,
  batch_size,
  field_names,
  dtype_map = NULL,
  max_examples = NULL,
  shuffle = FALSE,
  buffer_scaler = 10L,
  tensorflow = NULL
)

ndm_collect_tfrecord_batches(
  file,
  batch_size,
  field_names,
  dtype_map = NULL,
  max_batches = NULL,
  max_examples = NULL,
  shuffle = FALSE,
  buffer_scaler = 10L,
  tensorflow = NULL
)
}
\arguments{
\item{field_names}{Character vector of TFRecord field names that should be
parsed from each example.}

\item{dtype_map}{Optional named vector or list mapping `field_names` to
TensorFlow dtype names or TensorFlow dtype objects.}

\item{tensorflow}{Optional TensorFlow module. When omitted, ndm uses
the active backend's TensorFlow module when available and otherwise
imports TensorFlow from the active reticulate environment at call time.}

\item{file}{Path to the `.tfrecord` file to read.}

\item{batch_size}{Batch size used when constructing the TensorFlow dataset.}

\item{max_examples}{Optional maximum number of examples to consume from the
file before batching.}

\item{shuffle}{Logical scalar indicating whether the dataset should be
shuffled.}

\item{buffer_scaler}{Integer multiplier used to derive the TensorFlow shuffle
buffer size from `batch_size`.}

\item{max_batches}{Optional maximum number of batches to collect when using
`ndm_collect_tfrecord_batches()`.}
}
\value{
`ndm_create_tfrecord_parser()` returns a parser function compatible
  with `tf$data$Dataset$map()`. `ndm_read_tfrecord_dataset()` returns a
  TensorFlow dataset object. `ndm_collect_tfrecord_batches()` returns a list
  of parsed batches.
}
\description{
These wrappers construct parsers for the ndm TFRecord schema and use
them to build TensorFlow datasets or eagerly collect batches into R.
}
\details{
TFRecord parsing requires TensorFlow to be importable from the active
reticulate environment. In practice this usually means provisioning the
backend with `ndm_build_backend(include_tensorflow = TRUE)` and then calling
`ndm_initialize_backend(import_tensorflow = TRUE)` for the same conda
environment before reading datasets.
}
\examples{
fields <- ndm_tfrecord_fields_real()
parser <- ndm_create_tfrecord_parser(fields, ndm_tfrecord_dtype_map("real"))
is.function(parser)

}
