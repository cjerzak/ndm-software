% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multidisease_api.R
\name{ndm_bootstrap_multidisease_tfrecords}
\alias{ndm_bootstrap_multidisease_tfrecords}
\title{Bootstrap canonical multidisease TFRecords by BaseID}
\usage{
ndm_bootstrap_multidisease_tfrecords(
  project_root = getwd(),
  analysis_name = "RealLatest",
  grid = NULL,
  grid_file = file.path("Data", "RunGrids", "RealGrids", sprintf("RealGrid_\%s.csv",
    analysis_name)),
  base_ids = NULL,
  tfrecord_dir = file.path("Data", "RunTFRecords", "RealTFRecords", analysis_name),
  disease_names,
  data_format = "IHME",
  outcome_metric = "CountValue",
  data_subset = "all",
  lookahead = 12L,
  min_anchoring_time = 4L,
  training_target_horizon = NULL,
  producer = NULL,
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{project_root}{Project root containing multidisease data and grids.}

\item{analysis_name}{Analysis label used for default paths.}

\item{grid}{Optional in-memory multidisease grid.}

\item{grid_file}{Optional multidisease grid CSV path.}

\item{base_ids}{Optional BaseIDs to build, in requested order.}

\item{tfrecord_dir}{Canonical TFRecord output directory.}

\item{disease_names}{Non-empty disease names passed to the multidisease
loader.}

\item{data_format}{Multidisease source format: `"IHME"`, `"WHO"`, or
`"Tycho"`.}

\item{outcome_metric}{Outcome column name.}

\item{data_subset}{Multidisease location subset.}

\item{lookahead}{Tensor lookahead. This remains distinct from a grid row's
shorter `evaluationHorizon`.}

\item{min_anchoring_time}{Minimum training anchor time.}

\item{training_target_horizon}{Optional positive number of consecutive
post-anchor targets that must be finite for an anchor to be eligible for
training. It must not exceed `lookahead`; a same-named grid column is used
when this argument is `NULL`.}

\item{producer}{Non-empty named producer metadata. Required to publish.
For environment-bound training, use `list(contract = "<contract>")`.}

\item{overwrite}{Whether to replace an existing complete pair.}

\item{dry_run}{Whether to return the plan without loading data or writing.}
}
\value{
A data frame with one row per BaseID. `status` is `"planned"` for
  dry runs, `"written"` after publication, or `"skipped_existing"` only
  after an existing pair passes full contract and readability validation.
}
\description{
Builds one serial, canonical real-schema TFRecord pair per multidisease
`BaseID`. The bootstrap and training paths share the same raw-data loader,
table adapter, dataset specification, source fingerprint, data seed, and
complete-location support contract.
}
