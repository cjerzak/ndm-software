% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_api.R
\name{ndm_predict}
\alias{ndm_predict}
\alias{ndm_loss}
\title{Generate predictions or evaluate the training loss}
\usage{
ndm_predict(x, batch = NULL, inference = TRUE, seed = 1L, update_state = TRUE)

ndm_loss(
  x,
  batch = NULL,
  y = NULL,
  y_mask = NULL,
  iteration = 1L,
  seed = 1L,
  update_state = TRUE
)
}
\arguments{
\item{x}{Either an `ndm_model`, an `ndm_trained_model`, or a prepared runtime
environment that already contains the required runtime objects.}

\item{batch}{Optional batch object. Supply either a named TFRecord-style list
or the four-element packaged list returned by `ndm_batch_to_model_inputs()`.
When `NULL`, the runtime must already contain `batch_l_cal`.}

\item{inference}{Logical scalar indicating whether `ndm_predict()` should use
the inference prediction path or the training prediction path.}

\item{seed}{Integer seed used to derive the per-example JAX RNG keys.}

\item{update_state}{Logical scalar indicating whether the runtime `state`
object should be updated with the returned state.}

\item{y}{Optional observed outcome tensor passed to `ndm_loss()`. When
omitted, `YTrue_out` is inferred from `batch`.}

\item{y_mask}{Optional outcome mask tensor passed to `ndm_loss()`. When
omitted, `YTrue_out_mask` is inferred from `batch`.}

\item{iteration}{Training iteration number forwarded to the runtime loss
function.}
}
\value{
`ndm_predict()` returns the prediction object produced by the
  runtime. `ndm_loss()` returns the loss object produced by the runtime.
}
\description{
These helpers call the runtime prediction and loss functions stored in a
prepared runtime environment.
}
\examples{
\dontrun{
preds <- ndm_predict(model, batch = batch_l_cal)
loss <- ndm_loss(model, batch = batch_l_cal)
}

}
