% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_api.R
\name{ndm_build_model}
\alias{ndm_build_model}
\alias{print.ndm_model}
\alias{ndm_train}
\alias{print.ndm_trained_model}
\alias{ndm_fit}
\title{Build and train models with a local runtime}
\usage{
ndm_build_model(
  runtime_env,
  analysis_root = NULL,
  model_type = c("DecoderOnly", "NeuralODE"),
  model_spec = NULL,
  backbone = "transformer",
  runtime_globals = list()
)

\method{print}{ndm_model}(x, ...)

ndm_train(x, analysis_root = NULL, run_define = TRUE, run_loop = TRUE)

\method{print}{ndm_trained_model}(x, ...)

ndm_fit(
  config = ndm_create_config(),
  model_spec = NULL,
  data_generator = c("sim", "real"),
  runtime_globals = list(),
  data_globals = list(),
  build_globals = list(),
  train_globals = list(),
  run_define = TRUE,
  run_loop = TRUE
)
}
\arguments{
\item{runtime_env}{Runtime environment containing the sourced legacy helper
code and data globals.}

\item{analysis_root}{Root directory for the local analysis runtime.}

\item{model_type}{Model family to build. Either `"DecoderOnly"` or
`"NeuralODE"`.}

\item{model_spec}{Optional `ndm_model_spec` object used to override the model
TeX specification supplied to the runtime model builder.}

\item{backbone}{Backbone family. Phase 1 supports `"transformer"` only.}

\item{runtime_globals}{Named list of additional globals assigned during the
relevant runtime stage. `ndm_build_model()` uses them before building the
model, while `ndm_fit()` uses them before sourcing the runtime.}

\item{x}{Either an `ndm_model`, an `ndm_trained_model`, or a prepared runtime
environment, depending on the function being called.}

\item{...}{Unused; included for S3 method compatibility.}

\item{run_define}{Logical scalar indicating whether the training definition
script should be sourced.}

\item{run_loop}{Logical scalar indicating whether the training loop script
should be sourced.}

\item{config}{An object of class `ndm_config`, usually created by
`ndm_create_config()`.}

\item{data_generator}{Which local data generator to source before calling
`ndm_build_model()` inside `ndm_fit()`.}

\item{data_globals}{Named list of globals assigned before sourcing the data
generator in `ndm_fit()`.}

\item{build_globals}{Named list of globals assigned before building the model
in `ndm_fit()`.}

\item{train_globals}{Named list of globals assigned immediately before
training in `ndm_fit()`.}
}
\value{
`ndm_build_model()` returns an object of class `ndm_model`.
  `ndm_train()` and `ndm_fit()` return an object of class
  `ndm_trained_model`.

`print.ndm_model()` prints a brief summary of an `ndm_model` object
  and invisibly returns `x`.

`print.ndm_trained_model()` prints a brief summary of an
  `ndm_trained_model` object and invisibly returns `x`.
}
\description{
These wrappers layer a small R API over local Phase 1 model build and
training scripts.
}
\examples{
cfg <- ndm_create_config()
spec <- ndm_model_spec()
\dontrun{
runtime_env <- ndm_prepare_runtime(cfg)
ndm_prepare_data(runtime_env, generator = "sim")
model <- ndm_build_model(runtime_env, model_spec = spec)
trained <- ndm_train(model)
}

}
